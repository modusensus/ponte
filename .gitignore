# SSH 私钥与公钥 —— 绝不入库
id_rsa
id_rsa.pub

# Python 缓存 / 编译产物
__pycache__/
*.pyc
*.pyo

# 虚拟环境
.venv/
venv/

# 覆盖率产物
.coverage
coverage.xml
htmlcov/

# 可编辑安装产物
*.egg-info/

# 工具缓存
.pytest_cache/
.mypy_cache/
.ruff_cache/

# 打包产物
build/
dist/

# 本地真实配置：配置已迁出包目录，此处仅为兼容旧位置，绝不入库
ponte/config.toml

# ponte 守护进程运行时文件
ponte.pid
ponte.status.json
ponte.stop
ponte.log
ponte.log.*

# 开发临时文件
_scratch.py
_wtest.txt
_inspect.ps1
_fix_ponte_task.ps1
# SSH known_hosts（运行时自动写入，不入库）
known_hosts
known_hosts.old
